"# indian_bistro" 
# 🍽️ Indian Bistro

A simple **Node.js + Express** web application showcasing an Indian Bistro website with multiple pages (Home, Menu, Buffet, About), rendered using **EJS** templates.

This repository hosts the backend server for *Indian Bistro* — a restaurant-style (Bistro) site with clean routing, views, and static assets.

## Features

- Home page served at `/`
- Menu page and routes (`/menu`)
- Buffet information (`/buffet`)
- About page (`/about`)
- Views rendered using **EJS**
- Static files served from `/public`
- Configurable via **dotenv**

---

## 🛠️ Technologies Used

- **Node.js**
- **Express.js** 
- **EJS** 
- **dotenv**
- **JavaScript (ES Modules)**


##  Getting Started
### Prerequisites

Make sure you have installed:

- Node.js (v14+ recommended)
- npm (comes with Node.js)
- Git

---

### 📥 Installation

1. **Clone this repository**

    git clone https://github.com/smitaisverige/indian_bistro.git
  
2. **Navigate to the project directory**
    cd indian_bistro

3. **Install dependencies**
    npm install

4. **Create a `.env` file** in the project root and add:
    PORT=3000

5. **Start the server**
    npm start

---

##  Usage

Once the server is running:

- Visit: `http://localhost:3000/` in your browser  
- Explore the pages:
  - `http://localhost:3000/menu`
  - `http://localhost:3000/buffet`
  - `http://localhost:3000/about`



